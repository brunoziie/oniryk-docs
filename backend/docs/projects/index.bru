meta {
  name: index
  type: http
  seq: 1
}

get {
  url: {{HOST}}/projects
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

script:post-response {
  bru.setEnvVar('PROJECT_ID', res.body.data[0]?.id)
}
